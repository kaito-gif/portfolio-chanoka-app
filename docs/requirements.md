# portfolio-chanoka-app 実装範囲

**このファイルはリポジトリごと公開する前提で書く。** 実在の氏名・屋号・実ドメイン・単価は書かない。

最終更新: 2026-08-13（実装範囲の確定）

## 位置づけ

副業ポートフォリオ第3弾。第1弾 `portfolio-demo-store`（Shopifyテーマ実装）、
第2弾 `portfolio-booking-app`（Laravel/Filamentによる外部バックエンド）に続き、
**Shopifyのアプリ拡張領域**を担当する。

第1弾で構築した架空の日本茶D2C `chanoka` のデモストアに、実際にインストールして動かす。

## 何を作るか

**日本の贈答EC特有の「のし・ギフト対応」を、Shopifyアプリとして実装する。**

お茶は日本の贈答品の代表格であり、`chanoka` ブランドの世界観と必然的に噛み合う。
かつ熨斗・表書き・名入れ・外のし／内のしは Shopify 標準が構造的に持たない領域で、
日本の Shopify 案件で実際に頻出する要件でもある。

「技術を展示するための機能」ではなく「発注者が実際に困っていることを解く機能」を
選んだのが本作品の設計方針。

## 確定事項（2026-08-13）

| 論点 | 決定 | 理由 |
|---|---|---|
| 熨斗の指定単位 | **商品単位**（cart line attributes） | 「この茶葉は御中元、こちらは熨斗なし」が実務では起きる。Cart Transform の公式例も line attribute ベースで相性が良い。注文単位から商品単位への後付け拡張は困難なため最初から商品単位で作る |
| 入力UIの配置 | **カートページのみ** | 実装・検証が1箇所で済み、複数商品の熨斗をまとめて設定できて操作としても自然。チェックアウト内は Plus 限定で使えない |
| 公開のタイミング | **フェーズ2まで作り切ってから** | Functions を2種類（Cart Transform / Discount）踏め、購入から受注業務まで通しで見せられる状態になる |
| 配布形態 | **extension-only app / custom distribution** | バックエンド・OAuth・ホスティングが不要（費用0円で恒久的に動かせる）。App Store 公開の道は取らない |
| 数量2以上の行の扱い | **行単位で1つの熨斗**。のし料は数量分課金 | 別の熨斗にしたい場合は商品ページから別途カート投入すれば、属性が違うため Shopify が自動で別行にする。行より細かい粒度が要るのは「贈り先ごとに配送先が違う」場合だが、**複数配送先への別送はスコープ外**なので現時点では不要。別送を実装する日が来たら数量1への強制分割へ移行する必要がある |
| 受注後の訂正 | **注文メタフィールドに訂正値を持たせる**（フェーズ2） | 確定した注文の line item properties は編集できない。注文メタフィールドは更新可能なので、Admin block に訂正UIを置き、印字用データはメタフィールドがあればそちらを優先する |
| のし・包装料の税区分 | **茶葉は軽減税率、のし・包装料は標準税率**として扱う（※未検証） | 茶葉は飲食料品、有料の贈答用包装は役務提供という一般的な整理による。1注文に2つの税率が混在する。**税率の解釈もShopify側の設定手順もこの時点では未検証**であり、着手時に管理画面の実物と公的な情報で確認する |

## アーキテクチャ

**extension-only app** として構成する。Theme App Extension・Shopify Functions・Admin UI Extension
のみで構成し、開発者側のWebサーバーを持たない。Shopify が全てをホストする。

- 参考: [Build an extension-only app](https://shopify.dev/docs/apps/build/app-extensions/build-extension-only-app)
- 制約: extension-only app は **custom distribution 限定**。App Store での公開はできない
- 帰結: 月額のホスティング費用が発生しない。ポートフォリオは「今すぐ動いて見られる」ことが
  価値なので、維持コストが0であることを積極的に選んでいる

## 構成要素

### フェーズ1（コア。これだけで作品として成立する）

| # | 要素 | 技術 | 内容 |
|---|---|---|---|
| 1 | のし入力ブロック | Theme App Extension（App Block） | カート内の各行に「熨斗をつける」トグル。展開すると 表書き / 名入れ / 外のし・内のし を入力 |
| 2 | 表書きのマスタ管理 | Metaobject | 御中元・御歳暮・御祝・内祝・御礼・粗品・無地熨斗などをマーチャントが管理画面から増減できる。ハードコードしない |
| 3 | 入力値の保持 | cart line attributes（line item properties） | 注文まで自動で引き継がれる |
| 4 | のし・包装料の加算 | Cart Transform Function（expand operation） | 熨斗ありの行を expand し、のし・包装料のコンポーネントを追加する。課税設定・税込表示の整合を含む |
| 5 | 受注画面での熨斗確認 | Admin UI Extension（`admin.order-details.block.render`） | 注文詳細ページにカードとして常時表示。出荷担当が実際に使える形にする |

### フェーズ2（説得力の上乗せ。ここまでで公開）

| # | 要素 | 技術 | 内容 |
|---|---|---|---|
| 6 | 一定金額以上でのし・包装料を無料 | Discount Function | 当初「まとめ買い割引」として企画していた要素をここに統合する |
| 7 | 購入後の贈答内容の確認表示 | Checkout UI Extension（Thank you / Order status） | Starter 以外の全プランで利用可 |
| 8 | 熨斗の印字用データ出力 | Admin UI Extension 内 | 注文の熨斗情報を一覧・コピーできる形にする。訂正値があればそちらを優先 |
| 9 | 受注後の熨斗の訂正 | Admin UI Extension ＋ 注文メタフィールド | 表書きの誤字などをスタッフが直せるようにする。line item properties は変更不可のため、訂正値をメタフィールドへ保存する |
| 10 | Functions の自動テスト | fixture ベースの単体テスト | 金額を動かすロジック（のし料の加算・包装料無料の判定）は目視確認では不十分。「動いたように見えるが計算が合っていない」を防ぐ。実行方法は着手時に確認する |
| 11 | デモ資材の作成 | 動画・GIF・スクリーンショット | 下記「見せ方」参照。**これがフェーズ2の完了条件に含まれる** |

### フェーズ3（当面やらない）

配送希望日時の指定、メッセージカード文面、明細書の金額非表示、複数配送先への別送。
必要性が出たら再検討する。

## 見せ方（デモ資材）

**本作品の成果の大半は、管理画面かチェックアウトの中にしか現れない。**
第1弾はストアURLとパスワードを渡せば見せられ、第2弾はGitHubの公開リポジトリで
実装過程が追えたが、第3弾はどちらの方法でも Admin UI Extension と Functions の
動作を発注者に見せられない。管理画面のアクセス権を渡すことはできないため、
**デモ資材の作成を実装と同格の要件として扱う。**

最低限そろえるもの:

- カートで熨斗を指定 → のし・包装料が加算される流れ（画面録画）
- 一定金額到達で包装料が無料になる瞬間（画面録画）
- 注文詳細ページの熨斗情報カード（スクリーンショット）
- 熨斗の印字用データ出力と、受注後の訂正操作（スクリーンショットまたは画面録画）

これを後回しにすると、フェーズ2完了時点で「作ったが見せられない成果物」になる。

## 設計判断の理由（明文化）

**なぜ Cart Transform expand を使うのか（`/cart/add.js` で料金商品を直接追加しないのか）**

単に料金行を足すだけなら、フロントから のし・包装料商品をカートに追加する方が実装も
テストも単純である。それでも expand を選ぶ理由は次の2点。

- 別行として追加すると、**顧客がのし・包装料の行だけを削除できてしまう**
- 親商品を削除しても、**のし・包装料の行が孤児として残る**

expand は親行にコンポーネントとして束ねるため、この2つが構造的に起きない。
「Functionsを使いたいから使う」のではなく、整合性の担保が理由であることを
READMEにも明記する。

## やらないこと

- **チェックアウト内（情報・配送・支払いステップ）へのUI追加** … Shopify Plus 限定のため。
  ここに依存すると案件対象が狭まり、デモも動かせない可能性がある
- **App Store への申請** … プライバシーポリシー・サポート体制・GDPR webhook 等の審査対応は
  Shopify の実装力の証明とほぼ無関係で、費用対効果が悪い
- **第1弾・第2弾との技術統合** … スタックを意図的に分けているのが3作品の構成意図

## 検証すべき技術リスク（すべて未検証）

着手時に順に潰す。実機で確認するまで「できる」と書かない。

1. Cart Transform の expand で追加した「のし・包装料」が、カート／チェックアウトで
   どう表示されるか。expand は親をバンドル扱いにするため、Dawn 標準のカート表示と
   競合しないか
2. のし・包装料用のダミー商品（在庫追跡なし・非公開）を `chanoka-demo` に作る必要がある。
   expand には実在の variant ID が要るため
3. カート上で line item properties を後から編集する経路。`/cart/change.js` で properties を
   更新する際の行の同一性の扱い
4. Discount Function で「のし・包装料」のラインだけを100%割引の対象にする方法
5. app-owned Metaobject を Theme App Extension（Liquid / フロント）側から読む方法
6. 開発ストア `chanoka-demo` に custom distribution のアプリをインストールする手順。
   なお第三者レビューで「開発ストアなら custom distribution は不要では」との指摘を受けたが、
   公式ドキュメントに `Extension-only apps can only be installed with custom distribution.`
   と明記されているため、extension-only を選ぶ限り選択の余地はないと判断している。
   `shopify app dev` の開発時プレビューと恒久インストールの混同と見るが、実機で確認する
7. チェックアウト拡張のプラン境界（Plus限定の範囲）を着手時に公式ドキュメントで再確認する。
   Shopifyはこの手の境界を頻繁に見直すため、本ファイル記載時点の理解のまま進めない
8. Functions の単体テストの実行方法。fixture の与え方と、CI に載せられるか
9. 軽減税率の扱い。茶葉8% / のし・包装料10% という整理が妥当か（税務の領域なので
   公的な情報で確認する）と、Shopify 側で商品ごとに税率を分ける具体的な設定手順。
   **管理画面のメニュー名を記憶で書かない**
10. Admin UI Extension から注文メタフィールドへ書き込む方法と、必要なアクセススコープ

## 公開情報のガードレール

- デモデータ・スクリーンショット・テストコードの**名入れサンプルは架空名**を使う
  （「山田」「佐藤」等）。実在の氏名は書かない
- 第1弾・第2弾と同じ `chanoka` ブランドを使うため、一方の実名情報やヒントが
  他方に紛れ込んでいないかも確認対象にする
- 公開前のリーク検査は `~/.claude/portfolio/ng-words.txt`（git管理外）を使う

## 第1弾との接続

第1弾 `docs/context.md` に「産地・味わい・カフェインは商品説明のHTMLに `<li>` で
埋めているだけでタグにもメタフィールドにもなっていない」という積み残しがある。
本アプリで Metaobject / Metafield を扱う際、この整備を併せて行うかは着手時に判断する
（第1弾のリポジトリに手を入れることになるため、差分の意味が濁らないか要検討）。
