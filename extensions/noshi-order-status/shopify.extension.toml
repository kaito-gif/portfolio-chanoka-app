# Learn more about configuring your Customer account UI extension:
# https://shopify.dev/api/customer-account-ui-extensions/unstable/configuration

# The version of APIs your extension will receive. Learn more:
# https://shopify.dev/docs/api/usage/versioning
api_version = "2026-07"

[[extensions]]
name = "t:name"
handle = "noshi-order-status"
type = "ui_extension"
uid = "24ba697d-2ccc-1a15-08b4-ca64602341428da10d4f"
description = "t:description"

# Controls where in Shopify your extension will be injected,
# and the file that contains your extension’s source code. Learn more:
# https://shopify.dev/docs/api/customer-account-ui-extensions/unstable/extension-targets-overview

[[extensions.targeting]]
module = "./src/OrderStatusBlock.jsx"
target = "customer-account.order-status.cart-line-item.render-after"

# この拡張は Cart Lines API(shopify.target)だけで足り、Storefront APIも外部fetchも
# 使わないため、api_access・network_access のどちらも有効化しない(不要な権限は持たない)。
