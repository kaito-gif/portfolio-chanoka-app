query CartInput {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    lines {
      id
      quantity
      noshiTitle: attribute(key: "表書き") {
        value
      }
    }
  }
  discount {
    discountClasses
  }
}
